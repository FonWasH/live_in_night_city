local EASY = require("config/mode/Easy")
local NORMAL = require("config/mode/Normal")
local HARD = require("config/mode/Hard")

local Config = {
    [1] = EASY,
    [2] = NORMAL,
    [3] = HARD
}

return Config
